# pragyan itself makes no LLM calls and needs no AI credentials. All intelligence
# runs as the /pragyan-tick Claude Code skill over the MCP server (see CLAUDE.md):
# it maintains the living day report and goes deep on hot topics.
#
# Optional overrides (see api/config.json for defaults):
# XFEED_PORT=8787
# XFEED_DB_PATH=api/feed.db
# XFEED_KB_DIR=.kb
